{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["APP_ENV", "APP_RELEASE", "NEXT_PUBLIC_STOREFRONT_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"]
    },
    "dev": {
      "env": ["APP_ENV", "APP_RELEASE", "NEXT_PUBLIC_STOREFRONT_URL"]
    }
  }
}
